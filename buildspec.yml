version: 0.2

phases:
  install:
    commands:
      - echo Nothing to install
  build:
    commands:
      - echo Build started on `date`
artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: .  # root of your source code
